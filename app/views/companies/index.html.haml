%h1 My companies

%div{class: 'text-right'}
  = link_to 'Add company', new_user_company_path(@user), class: 'btn btn-primary'

%div
  %ul{class: 'company-items'}
    - @companies.each do |company|
      %li
        %div{class: 'row'}
          %div{class: 'col-sm-2'}
            = cl_image_tag((company.logo || 'company_logo.png'), alt: 'logo', with: 48, height: 48)
          %div{class: 'col-sm-10'}
            %div{class: 'company-item-title'}
              =link_to company.name, user_company_path(@user, id: company.id)

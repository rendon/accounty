- if object.errors.any?
  %div{class: 'error-explanation'}
    %div{class: 'alert alert-danger'}
      = "The form has #{pluralize(object.errors.count, 'error')} errors."
    %ul
      - object.errors.full_messages.each do |error|
        %li= error
